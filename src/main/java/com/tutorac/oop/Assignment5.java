package com.tutorac.oop;

public class Assignment5 {
    // DECLARE A CLASS CALLED AS CAR
    // DECLARE MEMBER VARIABLES (make, model, fuelType, engineCapacity, numberGears)
    // DECLARE METHODS TO startCar(), stopCar()
    // DECLARE METODS TO CALCULATE maxTravelDistance() BASED ON BELOW CONDITIONS
    // IF engineCapacity > 1500 and IF fuelType == PETROL
    //        mileage - 8KM / L
    // IF engineCapacity < 1500 and IF fuelType == PETROL
    //       mileage - 11KM / L
    // IF engineCapacity > 1500 and IF fuelType == DIESEL
    //       mileage - 13KM / L
    // IF engineCapacity < 1500 and IF fuelType == DIESEL
    //       mileage - 17KM / L
    // DECLARE ANOTHER METHOD CALLED AS changeGear() BASED ON BELOW CONDITIONS
    // if speed is > 0 and <= 30
    //       output - switched to gear 1
    // if speed is >30 and <=50
    //       output - switched to gear 2
    // if speed >50 and <=80
    //       output - switched to gear 3
}
